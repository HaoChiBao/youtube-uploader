# Phase 2 — HTTP API (not implemented)
#
# Planned endpoints:
#   POST /v1/jobs              — enqueue upload job
#   GET  /v1/jobs/{id}         — job status
#   GET  /v1/jobs              — list queue
#   POST /v1/channels/{id}/run — process pending batch
#   GET  /v1/channels/{id}/scheduled — list scheduled videos
#   POST /v1/channels/{id}/auth/start — OAuth URL
#
# Install optional deps: pip install '.[api]'
